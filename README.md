# Cloud Run Hello

This is a dummy server whose intent is to be deployed to [Google Cloud Run](
https://cloud.google.com/run/). The server itself doesn't do anything
interesting, but any supporting metadata will live next to it here.
